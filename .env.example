# Docker environment configuration
# Copy this to .env and modify values as needed

# Timezone
TZ=America/Los_Angeles

# Local poster directory path
LOCAL_POSTERS_PATH=                             #Path/to/you/loca/posters

# Remote server settings (these override config.yaml if set)
REMOTE_SERVER=                                  #IP address or hostname of your server
REMOTE_SHARE=                                   #Share name (from smbclient -L output)
REMOTE_USERNAME=                                #SMB username
REMOTE_PASSWORD=                                #SMB password
REMOTE_DOMAIN=                                  #Domain (WORKGROUP for local accounts)  

# Sync settings
WATCH_FOLDERS=                                  #Enable real-time folder monitoring
SYNC_INTERVAL=                                  #Seconds between sync checks (if not watching)
OVERWRITE_EXISTING=                             #Whether to overwrite existing remote posters

# Logging
LOG_LEVEL=INFO
